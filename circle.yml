machine:
  java:
    version: oraclejdk8
  gradle:
      version:2.14
  environment:
      APP_NAME: keycloak


dependencies:
  pre:
   - gradle --version
   - gradle wrapper
   - ./gradlew build distZip
  cache_directories:
    - ".gradle/"

test:
  override:
deployment:
  master:
    branch: master
    commands:
      - gradle dist
      - ./bin/deploy.sh
