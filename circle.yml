machine:
  environment:
    APP_NAME: keycloak
    java:
      version: openjdk8

dependencies:
  pre:
     - gradle dependencies
  cache_directories:
    - ".gradle/"

test:
  override:
    - gradle build
deployment:
  master:
    branch: master
    commands:
      - gradle dist
      - ./bin/deploy.sh
