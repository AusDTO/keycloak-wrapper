machine:
  java:
    version: oraclejdk8
  environment:
      APP_NAME: keycloak


dependencies:
  pre:
   - gradle build
  cache_directories:
    - ".gradle/"

test:
  override:
deployment:
  master:
    branch: master
    commands:
      - gradle dist
      - ./bin/deploy.sh
